#include "src/arithmetic.h"
#include "src/magic.h"
#include <bits/stdc++.h>
using namespace std;

int main(int argc, char *argv[]) {
   vector<int> nums = {1,2,5};
    math::sum(nums);
    return 0;
}